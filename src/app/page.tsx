import { Login } from "@/components";
import Image from "next/image";

export default function Home() {
  return (
    <main className="min-h-screen flex align-center justify-center">
        <Login></Login>
    </main>
  );
}
